# Archive

This folder is for historical notes only.

Archived docs are not source of truth. Agents should not read this folder by default.

When archiving an old doc, prefer either:

1. moving it here unchanged with a date prefix, or
2. replacing the old root-level file with a short pointer to the new source-of-truth doc.

Do not keep duplicate active specs in both archive and source-of-truth folders.
